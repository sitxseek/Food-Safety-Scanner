<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="itp341.guo.yangzong.foodsafetyscanner.ResultFragment"
    android:padding="12dp">
    <ProgressBar
        android:id="@+id/indeterminateBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true" />
    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:padding="16dp"
        android:id="@+id/allergy_image"
        android:nextFocusDown="@+id/safe_label" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/safe_label"
        android:layout_below="@+id/allergy_image"
        android:layout_marginTop="8dp"
        android:layout_centerHorizontal="true"
        android:textSize="28sp"
        android:nextFocusUp="@+id/allergy_image"
        android:nextFocusDown="@+id/result_text"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:id="@+id/result_text"
        android:layout_below="@+id/safe_label"
        android:textSize="18sp"
        android:padding="8dp"
        android:nextFocusUp="@+id/safe_label"
        android:nextFocusDown="@+id/details"
        android:background="@color/colorGrey"/>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/back"
        android:text="@string/back"
        android:backgroundTint="@color/colorBlue"
        android:textColor="@color/colorWhite"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="24dp"
        android:layout_centerHorizontal="true"
        android:nextFocusUp="@+id/result_text" />
</RelativeLayout>
